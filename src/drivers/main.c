#include "stm32h7xx.h"  // CMSIS / HAL headers

int main(void) {
    // Initialize HAL if you use it
    // HAL_Init();

    while (1) {
        // Infinite loop for embedded program
    }

    return 0; // Never reached
}
